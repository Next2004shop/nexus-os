# Nexus Cloud - Next.js Frontend

This is the new cloud-native frontend for Nexus AI, built with **Next.js 14**, **Tailwind CSS**, and **TypeScript**.

## 🚀 Getting Started

1.  Navigate to the frontend directory:
    ```bash
    cd frontend
    ```

2.  Install dependencies (if not already done):
    ```bash
    npm install
    ```

3.  Start the development server:
    ```bash
    npm run dev
    ```

4.  Open [http://localhost:3000](http://localhost:3000) with your browser.

## 🏗️ Architecture

-   **Framework**: Next.js 14 (App Router)
-   **Styling**: Tailwind CSS + Custom Nexus Theme
-   **Icons**: Lucide React
-   **Charts**: Lightweight Charts (TradingView)
-   **Deployment**: Ready for Google Cloud Run / Vercel

## 📁 Structure

-   `app/`: Application routes (Trade, Stocks, AI Bot, Wallet)
-   `components/`: Reusable UI components
-   `lib/`: Utilities and API hooks
-   `public/`: Static assets
